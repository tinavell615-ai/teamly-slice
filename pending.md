# Карантин примеров

Сюда попадают частные случаи, обнаруженные в ходе работы и **не чинящиеся
на месте** (протокол работы над кодом, закон 9).

## Правила

- Строка добавляется, когда отладка одного примера идёт дольше 15 минут
  либо когда пример не относится к текущему слою.
- Поштучно ничего не чинится. Накопленный **класс** закрывается одним
  пакетом, когда до него доходит очередь по слоям.
- Строка удаляется только после того, как пример проходит на сохранённом
  образце. «Вроде заработало» основанием не является.
- Файл просматривается в начале каждой сессии и в конце каждого пакета.

## Классы

`тождество имён` · `границы кусков` · `резолв связей` · `коды свойств`
· `тела карточек` · `фазы извлечения` · `интерфейс`

Новый класс заводится, только если пример не ложится ни в один существующий.

---

## Открытые

| Дата | Что | Ожидалось | Получилось | Класс |
|---|---|---|---|---|
| 26.07 | Проверка дубля персонажа по подстроке: «Влад» внутри «Владимир» | разные карточки | ложное предупреждение о дубле | тождество имён |
| 26.07 | Полная замена тела карточки: endpoint неизвестен, разведка дала только запись свойств | `ТЕЛО:` заменяет содержимое | режим отклоняется парсером, доступно только `ТЕЛО-ДОПОЛНИТЬ` | тела карточек |
| 26.07 | Значение селекта, которого нет среди вариантов колонки | вариант добавляется по решению автора | запись карточки отклоняется с указанием отсутствующего варианта | коды свойств |
| 26.07 | Пустые таблицы: нет строк → schemaProperties не читается, endpoint удаления неизвестен | коды прочитаны | missing, техстрока не создаётся | коды свойств |

## Закрытые

| Дата | Что | Класс | Чем закрыто |
|---|---|---|---|
| 26.07 | «Том» и «Том Реддл» создают две карточки вместо одной | тождество имён | Слой 1, задача 6: два алгоритма сравнения имён сведены в один (`names.py`), подстрочный удалён |
| 26.07 | Форма payload `property_update` с `articleId` неизвестна, обновление свойств не работает | коды свойств | Разведка 26.07: перехват из браузера дал точную форму команды, перебор кодов удалён |
